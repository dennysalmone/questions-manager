export enum EQuestionType {
    single = 'single',
    multi = 'multi',
    open = 'open'
}

export enum EActionType {
    edit = 'edit',
    create = 'create'
}

export enum ECardType {
    management = 'management',
    list = 'list',
}